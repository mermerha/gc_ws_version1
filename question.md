# 问题：机械爪舵机问题总结与排查手册

本文档记录了在任务开发过程中遇到的关于总线舵机及其控制系统的各类问题、现象、原因及解决方案。

---

## 1. 软件逻辑问题

### 1.1 模块导入失败 (ModuleNotFoundError)
*   **现象**：启动 `claw_service_node.py` 时报错 `No module named 'uservo'`。
*   **原因**：ROS 启动 Python 脚本时，其搜索路径不包含脚本所在目录，导致无法加载同目录下的 `uservo.py` SDK。
*   **解决方法**：在代码开头动态添加路径：
    ```python
    import sys, os
    sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
    ```

### 1.2 启动卡死 / 初始化极慢
*   **现象**：`roslaunch` 之后程序长时间停留在 `started with pid`，无后续输出。
*   **原因**：`UartServoManager` 默认会扫描 0-253 所有可能的 ID。由于设置了 1.0s 的串口超时，每扫描一个不存在的 ID 都会卡顿 1 秒，全量扫描需 4 分钟。
*   **解决方法**：
    -   禁用自动扫描：`is_scan_servo=False`。
    -   精准连接：手动执行 `manager.ping(servo_id)` 仅连接指定舵机。
    -   优化超时：将串口 `timeout` 缩短至 0.1s。

---

## 2. 硬件与电气问题 (核心难点)

### 2.1 串口 I/O 错误 (Errno 5 / Input/output error)
*   **现象**：`Failed to init servo on /dev/ttyUSB0: [Errno 5] Input/output error`。
*   **底层原因**：通过 `dmesg` 观察到内核报错 `-110 (USB Timeout)`。这意味着 CH341 串口芯片已停止响应或硬件链路中断。
*   **物理诱因**：
    -   **浪涌电流**：舵机启动瞬间电流过大，拉低了 USB 总线电压。
    -   **电磁干扰**：电机转动产生感应电动势（反向高压），打死串口芯片。
*   **解决方法**：
    -   **临时**：物理拔插 USB 线，强制重置芯片状态。
    -   **根治**：**独立供电**。舵机使用独立电源（如 UBEC 或 2S 电池），仅与 NX 共地，不共用正极。
    -   **升级**：更换抗干扰能力更强的 CP2102 或 FT232 串口模块。

### 2.2 虚拟测试需求
*   **需求**：在不起飞的情况下验证全流程逻辑。
*   **解决方案**：开发 `mock_odom.py` 模拟器。
    -   模拟 `/ekf/ekf_odom` 坐标变化，欺骗状态机使其认为飞机正在移动。
    -   配合 `mission_control` 的日志输出，在地面即可观察到抓球/投球动作的触发。

---

## 3. 开发建议
*   **安全第一**：在调试涉及物理动作的代码时，建议先使用 `mock_odom.py` 在地面完成所有逻辑验证。
*   **日志先行**：在初始化等关键位置保留 `rospy.loginfo` 步进日志，方便快速定位硬件死锁位置。
*   **硬件加固**：无人机震动大、气流复杂，务必确保 USB 转串口模块与舵机转接板之间的连接加固（如使用热熔胶或扎带）。
